events {}
http {
  server {
    listen 80;
    location /api/metrics { proxy_pass http://collector:6120/metrics; proxy_set_header Host $host; }
    location / { proxy_pass http://dashboard:3000; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; }
  }
}
